# SentinelVision backend environment
# Copy to .env and adjust as needed.

# Risk thresholds (0.0 - 1.0)
THRESHOLD_BLOCK=0.85
THRESHOLD_REVIEW=0.45

# Optional: per-category block overrides (category:threshold,...)
# CATEGORY_BLOCK_OVERRIDES=sexual_content:0.7,violence:0.8

# Models (Hugging Face IDs)
CLIP_MODEL_ID=openai/clip-vit-base-patch32
CAPTION_MODEL_ID=Salesforce/blip-image-captioning-base

# Device: cpu or cuda
DEVICE=cpu

# Upload limit (MB)
MAX_UPLOAD_MB=10

# App
DEBUG=false
